spring:
  application:
    name: job-service
server:
  port: 8081

management:
  endpoint:
    web:
        exposure:
            include: health,info
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: notification-service
server:
  port: 8084
management:
  endpoint:
    web:
      exposure:
        include: health,info
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


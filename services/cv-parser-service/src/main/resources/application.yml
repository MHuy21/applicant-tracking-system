spring:
  application:
    name: cv-parser-service

server:
  port: 8085
management:
  endpoint:
    web:
      exposure:
        include: health,info
eureka:
  client:
    service-url:
        defaultZone: http://localhost:8761/eureka/
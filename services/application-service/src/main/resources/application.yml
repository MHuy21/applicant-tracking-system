spring:
  application:
    name: application-service
server:
  port: 8083
management:
  endpoint:
    web:
        exposure:
            include: health,info
eureka:
  client:
    service-url:
        defaultZone: http://localhost:8761/eureka/
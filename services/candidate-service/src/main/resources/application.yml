spring:
  application:
    name: candidate-service
server:
  port: 8082
management:
  endpoint:
    web:
        exposure:
            include: health,info
eureka:
  client:
    service-url:
        defaultZone: http://localhost:8761/eureka/